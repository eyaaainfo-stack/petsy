<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 🔵 zdinahom: bch el app tnajjam ta5od el location 7a9i9iya
         (geolocator package) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- 🔵 ZID (fix el crash): "image_picker" te7taj hedhi el permission
         MDECLARÉE fel manifest bch tنجم tel9a el kamera (ImageSource.camera)
         - lowkan mch mawjouda, l'app te-crash direct (b'silence, tokhrej)
         ki el user y5tar "Camera" (Gallery ta5dem bla ha, chrahtha
         mch nafs el chemin). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- 🔵 ZID: bch el app tنجم tse2al el user "Allow this app to
         ignore battery optimizations?" (dialog systeme, mch settings
         b idek) - hedhi el permission LEZEMHA t-declari fel manifest
         bech el Intent (REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, fel
         MainActivity.kt) ye5dem. Google t9bel hedhi 3ala apps
         eli lezemhom real-time (VoIP, tracking, etc.) - w petsy
         3andha geolocator fa justifiable. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <application
        android:label="frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
    <!-- 🔵 ZID: "required=false" - bch el app teb9a installable 7ata
         3al appareils elli ma3andhomch kamera (mch obligatoire, ghir
         "on l'a besoin ken mawjouda"). -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
</manifest>